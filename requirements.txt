# MLflow Core
# Main MLflow package for experiment tracking and model registry
mlflow>=2.8.0

# PostgreSQL Database Adapter
# Required for MLflow to connect to PostgreSQL backend store
# Using binary version to avoid compilation issues
psycopg2-binary>=2.9.9

# AWS SDK for Python (Boto3)
# Required for S3-compatible storage (MinIO) interaction
# MLflow uses boto3 to interact with S3/MinIO for artifact storage
boto3>=1.28.0

# Additional Recommended Packages
# =================================

# NumPy - Scientific computing (often used with MLflow)
numpy>=1.24.0

# Requests - HTTP library (used by MLflow for API calls)
requests>=2.31.0

# PyYAML - YAML parser (for configuration files)
pyyaml>=6.0

# SQLAlchemy - SQL toolkit (optional, for advanced database operations)
# sqlalchemy>=2.0.0

# Pandas - Data manipulation (optional, useful for ML workflows)
pandas>=2.0.0

# Scikit-learn - Machine learning library (optional, for model logging examples)
scikit-learn>=1.3.0

# Matplotlib - Plotting library (for visualization in ML workflows)
matplotlib>=3.7.0

# Note: Additional ML framework packages can be added as needed:
# - tensorflow (for TensorFlow models)
# - torch (for PyTorch models)
# - xgboost (for XGBoost models)
# - lightgbm (for LightGBM models)
# etc.

